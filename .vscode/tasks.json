{
	"tasks": [
		{
			"label": "build",
			"type": "shell",
			"command": "meson setup --reconfigure -Dbuildtype=debugoptimized -Db_sanitize=address,undefined builddir && ninja -C builddir"
		}
	],
	"version": "2.0.0"
}
